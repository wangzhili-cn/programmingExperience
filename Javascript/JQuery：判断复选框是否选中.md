---
判断复选框是否选中，元素是否隐藏，prop相对attr有那些应用情况
---  
```
判断复选框是否选中  
checked='true'  
if($("#abc").is(":checked")'))  
是选中返回true，否则返回false  
判断是否隐藏  
$("#div").is(":hidden");  
prop应对于标签本身的属性变化，可以重复多次变化。  
attr相对于自定义的属性，对于标签自带属性不能多次变化。  
